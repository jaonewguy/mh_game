{
  "name": "chroma",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "a game about finding color",
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/**/*.test.mjs",
    "test:unit": "node --test tests/unit/**/*.test.mjs",
    "test:e2e": "node --test --test-concurrency=1 tests/e2e/**/*.test.mjs"
  }
}
